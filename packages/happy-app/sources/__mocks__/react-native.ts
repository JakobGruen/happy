// Minimal React Native stub for Vitest (Node.js environment).
// Only exports what is needed for modules under test.
export const Platform = { OS: 'ios' };
export const useWindowDimensions = () => ({ width: 390, height: 844 });
export const View = 'View';
export const StyleSheet = { create: (s: unknown) => s, flatten: (s: unknown) => s };
